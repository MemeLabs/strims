// Code generated by Wire protocol buffer compiler, do not edit.
// Source: BrokerPeerInitRequest in vpn.proto
package gg.strims.ppspp.proto

import com.squareup.wire.FieldEncoding
import com.squareup.wire.Message
import com.squareup.wire.ProtoAdapter
import com.squareup.wire.ProtoReader
import com.squareup.wire.ProtoWriter
import com.squareup.wire.Syntax.PROTO_3
import com.squareup.wire.WireField
import com.squareup.wire.internal.immutableCopyOf
import kotlin.Any
import kotlin.AssertionError
import kotlin.Boolean
import kotlin.Deprecated
import kotlin.DeprecationLevel
import kotlin.Int
import kotlin.Long
import kotlin.Nothing
import kotlin.String
import kotlin.collections.List
import kotlin.hashCode
import kotlin.jvm.JvmField
import okio.ByteString

class BrokerPeerInitRequest(
  @field:WireField(
    tag = 1,
    adapter = "com.squareup.wire.ProtoAdapter#UINT64",
    label = WireField.Label.OMIT_IDENTITY,
    jsonName = "peerId"
  )
  val peer_id: Long = 0L,
  @field:WireField(
    tag = 2,
    adapter = "com.squareup.wire.ProtoAdapter#BOOL",
    label = WireField.Label.OMIT_IDENTITY,
    jsonName = "preferSender"
  )
  val prefer_sender: Boolean = false,
  keys: List<ByteString> = emptyList(),
  unknownFields: ByteString = ByteString.EMPTY
) : Message<BrokerPeerInitRequest, Nothing>(ADAPTER, unknownFields) {
  @field:WireField(
    tag = 3,
    adapter = "com.squareup.wire.ProtoAdapter#BYTES",
    label = WireField.Label.REPEATED
  )
  val keys: List<ByteString> = immutableCopyOf("keys", keys)

  @Deprecated(
    message = "Shouldn't be used in Kotlin",
    level = DeprecationLevel.HIDDEN
  )
  override fun newBuilder(): Nothing = throw AssertionError()

  override fun equals(other: Any?): Boolean {
    if (other === this) return true
    if (other !is BrokerPeerInitRequest) return false
    if (unknownFields != other.unknownFields) return false
    if (peer_id != other.peer_id) return false
    if (prefer_sender != other.prefer_sender) return false
    if (keys != other.keys) return false
    return true
  }

  override fun hashCode(): Int {
    var result = super.hashCode
    if (result == 0) {
      result = unknownFields.hashCode()
      result = result * 37 + peer_id.hashCode()
      result = result * 37 + prefer_sender.hashCode()
      result = result * 37 + keys.hashCode()
      super.hashCode = result
    }
    return result
  }

  override fun toString(): String {
    val result = mutableListOf<String>()
    result += """peer_id=$peer_id"""
    result += """prefer_sender=$prefer_sender"""
    if (keys.isNotEmpty()) result += """keys=$keys"""
    return result.joinToString(prefix = "BrokerPeerInitRequest{", separator = ", ", postfix = "}")
  }

  fun copy(
    peer_id: Long = this.peer_id,
    prefer_sender: Boolean = this.prefer_sender,
    keys: List<ByteString> = this.keys,
    unknownFields: ByteString = this.unknownFields
  ): BrokerPeerInitRequest = BrokerPeerInitRequest(peer_id, prefer_sender, keys, unknownFields)

  companion object {
    @JvmField
    val ADAPTER: ProtoAdapter<BrokerPeerInitRequest> = object : ProtoAdapter<BrokerPeerInitRequest>(
      FieldEncoding.LENGTH_DELIMITED, 
      BrokerPeerInitRequest::class, 
      "type.googleapis.com/BrokerPeerInitRequest", 
      PROTO_3, 
      null
    ) {
      override fun encodedSize(value: BrokerPeerInitRequest): Int {
        var size = value.unknownFields.size
        if (value.peer_id != 0L) size += ProtoAdapter.UINT64.encodedSizeWithTag(1, value.peer_id)
        if (value.prefer_sender != false) size += ProtoAdapter.BOOL.encodedSizeWithTag(2,
            value.prefer_sender)
        size += ProtoAdapter.BYTES.asRepeated().encodedSizeWithTag(3, value.keys)
        return size
      }

      override fun encode(writer: ProtoWriter, value: BrokerPeerInitRequest) {
        if (value.peer_id != 0L) ProtoAdapter.UINT64.encodeWithTag(writer, 1, value.peer_id)
        if (value.prefer_sender != false) ProtoAdapter.BOOL.encodeWithTag(writer, 2,
            value.prefer_sender)
        ProtoAdapter.BYTES.asRepeated().encodeWithTag(writer, 3, value.keys)
        writer.writeBytes(value.unknownFields)
      }

      override fun decode(reader: ProtoReader): BrokerPeerInitRequest {
        var peer_id: Long = 0L
        var prefer_sender: Boolean = false
        val keys = mutableListOf<ByteString>()
        val unknownFields = reader.forEachTag { tag ->
          when (tag) {
            1 -> peer_id = ProtoAdapter.UINT64.decode(reader)
            2 -> prefer_sender = ProtoAdapter.BOOL.decode(reader)
            3 -> keys.add(ProtoAdapter.BYTES.decode(reader))
            else -> reader.readUnknownField(tag)
          }
        }
        return BrokerPeerInitRequest(
          peer_id = peer_id,
          prefer_sender = prefer_sender,
          keys = keys,
          unknownFields = unknownFields
        )
      }

      override fun redact(value: BrokerPeerInitRequest): BrokerPeerInitRequest = value.copy(
        unknownFields = ByteString.EMPTY
      )
    }

    private const val serialVersionUID: Long = 0L
  }
}
