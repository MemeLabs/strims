// Code generated by Wire protocol buffer compiler, do not edit.
// Source: PeerInit in vpn.proto
package gg.strims.ppspp.proto

import com.squareup.wire.FieldEncoding
import com.squareup.wire.Message
import com.squareup.wire.ProtoAdapter
import com.squareup.wire.ProtoReader
import com.squareup.wire.ProtoWriter
import com.squareup.wire.Syntax.PROTO_3
import com.squareup.wire.WireField
import com.squareup.wire.internal.sanitize
import kotlin.Any
import kotlin.AssertionError
import kotlin.Boolean
import kotlin.Deprecated
import kotlin.DeprecationLevel
import kotlin.Int
import kotlin.Long
import kotlin.Nothing
import kotlin.String
import kotlin.hashCode
import kotlin.jvm.JvmField
import okio.ByteString

/**
 * ---
 */
class PeerInit(
  @field:WireField(
    tag = 1,
    adapter = "com.squareup.wire.ProtoAdapter#UINT32",
    label = WireField.Label.OMIT_IDENTITY,
    jsonName = "protocolVersion"
  )
  val protocol_version: Int = 0,
  @field:WireField(
    tag = 2,
    adapter = "gg.strims.ppspp.proto.Certificate#ADAPTER",
    label = WireField.Label.OMIT_IDENTITY
  )
  val certificate: Certificate? = null,
  @field:WireField(
    tag = 3,
    adapter = "com.squareup.wire.ProtoAdapter#STRING",
    label = WireField.Label.OMIT_IDENTITY,
    jsonName = "nodePlatform"
  )
  val node_platform: String = "",
  @field:WireField(
    tag = 4,
    adapter = "com.squareup.wire.ProtoAdapter#STRING",
    label = WireField.Label.OMIT_IDENTITY,
    jsonName = "nodeVersion"
  )
  val node_version: String = "",
  unknownFields: ByteString = ByteString.EMPTY
) : Message<PeerInit, Nothing>(ADAPTER, unknownFields) {
  @Deprecated(
    message = "Shouldn't be used in Kotlin",
    level = DeprecationLevel.HIDDEN
  )
  override fun newBuilder(): Nothing = throw AssertionError()

  override fun equals(other: Any?): Boolean {
    if (other === this) return true
    if (other !is PeerInit) return false
    if (unknownFields != other.unknownFields) return false
    if (protocol_version != other.protocol_version) return false
    if (certificate != other.certificate) return false
    if (node_platform != other.node_platform) return false
    if (node_version != other.node_version) return false
    return true
  }

  override fun hashCode(): Int {
    var result = super.hashCode
    if (result == 0) {
      result = unknownFields.hashCode()
      result = result * 37 + protocol_version.hashCode()
      result = result * 37 + certificate.hashCode()
      result = result * 37 + node_platform.hashCode()
      result = result * 37 + node_version.hashCode()
      super.hashCode = result
    }
    return result
  }

  override fun toString(): String {
    val result = mutableListOf<String>()
    result += """protocol_version=$protocol_version"""
    if (certificate != null) result += """certificate=$certificate"""
    result += """node_platform=${sanitize(node_platform)}"""
    result += """node_version=${sanitize(node_version)}"""
    return result.joinToString(prefix = "PeerInit{", separator = ", ", postfix = "}")
  }

  fun copy(
    protocol_version: Int = this.protocol_version,
    certificate: Certificate? = this.certificate,
    node_platform: String = this.node_platform,
    node_version: String = this.node_version,
    unknownFields: ByteString = this.unknownFields
  ): PeerInit = PeerInit(protocol_version, certificate, node_platform, node_version, unknownFields)

  companion object {
    @JvmField
    val ADAPTER: ProtoAdapter<PeerInit> = object : ProtoAdapter<PeerInit>(
      FieldEncoding.LENGTH_DELIMITED, 
      PeerInit::class, 
      "type.googleapis.com/PeerInit", 
      PROTO_3, 
      null
    ) {
      override fun encodedSize(value: PeerInit): Int {
        var size = value.unknownFields.size
        if (value.protocol_version != 0) size += ProtoAdapter.UINT32.encodedSizeWithTag(1,
            value.protocol_version)
        if (value.certificate != null) size += Certificate.ADAPTER.encodedSizeWithTag(2,
            value.certificate)
        if (value.node_platform != "") size += ProtoAdapter.STRING.encodedSizeWithTag(3,
            value.node_platform)
        if (value.node_version != "") size += ProtoAdapter.STRING.encodedSizeWithTag(4,
            value.node_version)
        return size
      }

      override fun encode(writer: ProtoWriter, value: PeerInit) {
        if (value.protocol_version != 0) ProtoAdapter.UINT32.encodeWithTag(writer, 1,
            value.protocol_version)
        if (value.certificate != null) Certificate.ADAPTER.encodeWithTag(writer, 2,
            value.certificate)
        if (value.node_platform != "") ProtoAdapter.STRING.encodeWithTag(writer, 3,
            value.node_platform)
        if (value.node_version != "") ProtoAdapter.STRING.encodeWithTag(writer, 4,
            value.node_version)
        writer.writeBytes(value.unknownFields)
      }

      override fun decode(reader: ProtoReader): PeerInit {
        var protocol_version: Int = 0
        var certificate: Certificate? = null
        var node_platform: String = ""
        var node_version: String = ""
        val unknownFields = reader.forEachTag { tag ->
          when (tag) {
            1 -> protocol_version = ProtoAdapter.UINT32.decode(reader)
            2 -> certificate = Certificate.ADAPTER.decode(reader)
            3 -> node_platform = ProtoAdapter.STRING.decode(reader)
            4 -> node_version = ProtoAdapter.STRING.decode(reader)
            else -> reader.readUnknownField(tag)
          }
        }
        return PeerInit(
          protocol_version = protocol_version,
          certificate = certificate,
          node_platform = node_platform,
          node_version = node_version,
          unknownFields = unknownFields
        )
      }

      override fun redact(value: PeerInit): PeerInit = value.copy(
        certificate = value.certificate?.let(Certificate.ADAPTER::redact),
        unknownFields = ByteString.EMPTY
      )
    }

    private const val serialVersionUID: Long = 0L
  }
}
