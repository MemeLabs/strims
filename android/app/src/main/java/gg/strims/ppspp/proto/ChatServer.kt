// Code generated by Wire protocol buffer compiler, do not edit.
// Source: strims.chat.v1.ChatServer in chat/v1/chat.proto
package gg.strims.ppspp.proto

import com.squareup.wire.FieldEncoding
import com.squareup.wire.Message
import com.squareup.wire.ProtoAdapter
import com.squareup.wire.ProtoReader
import com.squareup.wire.ProtoWriter
import com.squareup.wire.Syntax.PROTO_3
import com.squareup.wire.WireField
import gg.strims.type.Key
import kotlin.Any
import kotlin.AssertionError
import kotlin.Boolean
import kotlin.Deprecated
import kotlin.DeprecationLevel
import kotlin.Int
import kotlin.Long
import kotlin.Nothing
import kotlin.String
import kotlin.hashCode
import kotlin.jvm.JvmField
import okio.ByteString

class ChatServer(
  @field:WireField(
    tag = 1,
    adapter = "com.squareup.wire.ProtoAdapter#UINT64",
    label = WireField.Label.OMIT_IDENTITY
  )
  val id: Long = 0L,
  @field:WireField(
    tag = 2,
    adapter = "com.squareup.wire.ProtoAdapter#BYTES",
    label = WireField.Label.OMIT_IDENTITY,
    jsonName = "networkKey"
  )
  val network_key: ByteString = ByteString.EMPTY,
  @field:WireField(
    tag = 3,
    adapter = "gg.strims.type.Key#ADAPTER",
    label = WireField.Label.OMIT_IDENTITY
  )
  val key: Key? = null,
  @field:WireField(
    tag = 4,
    adapter = "gg.strims.ppspp.proto.ChatRoom#ADAPTER",
    label = WireField.Label.OMIT_IDENTITY,
    jsonName = "chatRoom"
  )
  val chat_room: ChatRoom? = null,
  unknownFields: ByteString = ByteString.EMPTY
) : Message<ChatServer, Nothing>(ADAPTER, unknownFields) {
  @Deprecated(
    message = "Shouldn't be used in Kotlin",
    level = DeprecationLevel.HIDDEN
  )
  override fun newBuilder(): Nothing = throw AssertionError()

  override fun equals(other: Any?): Boolean {
    if (other === this) return true
    if (other !is ChatServer) return false
    if (unknownFields != other.unknownFields) return false
    if (id != other.id) return false
    if (network_key != other.network_key) return false
    if (key != other.key) return false
    if (chat_room != other.chat_room) return false
    return true
  }

  override fun hashCode(): Int {
    var result = super.hashCode
    if (result == 0) {
      result = unknownFields.hashCode()
      result = result * 37 + id.hashCode()
      result = result * 37 + network_key.hashCode()
      result = result * 37 + key.hashCode()
      result = result * 37 + chat_room.hashCode()
      super.hashCode = result
    }
    return result
  }

  override fun toString(): String {
    val result = mutableListOf<String>()
    result += """id=$id"""
    result += """network_key=$network_key"""
    if (key != null) result += """key=$key"""
    if (chat_room != null) result += """chat_room=$chat_room"""
    return result.joinToString(prefix = "ChatServer{", separator = ", ", postfix = "}")
  }

  fun copy(
    id: Long = this.id,
    network_key: ByteString = this.network_key,
    key: Key? = this.key,
    chat_room: ChatRoom? = this.chat_room,
    unknownFields: ByteString = this.unknownFields
  ): ChatServer = ChatServer(id, network_key, key, chat_room, unknownFields)

  companion object {
    @JvmField
    val ADAPTER: ProtoAdapter<ChatServer> = object : ProtoAdapter<ChatServer>(
      FieldEncoding.LENGTH_DELIMITED, 
      ChatServer::class, 
      "type.googleapis.com/strims.chat.v1.ChatServer", 
      PROTO_3, 
      null
    ) {
      override fun encodedSize(value: ChatServer): Int {
        var size = value.unknownFields.size
        if (value.id != 0L) size += ProtoAdapter.UINT64.encodedSizeWithTag(1, value.id)
        if (value.network_key != ByteString.EMPTY) size += ProtoAdapter.BYTES.encodedSizeWithTag(2,
            value.network_key)
        if (value.key != null) size += Key.ADAPTER.encodedSizeWithTag(3, value.key)
        if (value.chat_room != null) size += ChatRoom.ADAPTER.encodedSizeWithTag(4, value.chat_room)
        return size
      }

      override fun encode(writer: ProtoWriter, value: ChatServer) {
        if (value.id != 0L) ProtoAdapter.UINT64.encodeWithTag(writer, 1, value.id)
        if (value.network_key != ByteString.EMPTY) ProtoAdapter.BYTES.encodeWithTag(writer, 2,
            value.network_key)
        if (value.key != null) Key.ADAPTER.encodeWithTag(writer, 3, value.key)
        if (value.chat_room != null) ChatRoom.ADAPTER.encodeWithTag(writer, 4, value.chat_room)
        writer.writeBytes(value.unknownFields)
      }

      override fun decode(reader: ProtoReader): ChatServer {
        var id: Long = 0L
        var network_key: ByteString = ByteString.EMPTY
        var key: Key? = null
        var chat_room: ChatRoom? = null
        val unknownFields = reader.forEachTag { tag ->
          when (tag) {
            1 -> id = ProtoAdapter.UINT64.decode(reader)
            2 -> network_key = ProtoAdapter.BYTES.decode(reader)
            3 -> key = Key.ADAPTER.decode(reader)
            4 -> chat_room = ChatRoom.ADAPTER.decode(reader)
            else -> reader.readUnknownField(tag)
          }
        }
        return ChatServer(
          id = id,
          network_key = network_key,
          key = key,
          chat_room = chat_room,
          unknownFields = unknownFields
        )
      }

      override fun redact(value: ChatServer): ChatServer = value.copy(
        key = value.key?.let(Key.ADAPTER::redact),
        chat_room = value.chat_room?.let(ChatRoom.ADAPTER::redact),
        unknownFields = ByteString.EMPTY
      )
    }

    private const val serialVersionUID: Long = 0L
  }
}
