// Code generated by Wire protocol buffer compiler, do not edit.
// Source: LoadSessionResponse in profile.proto
package gg.strims.ppspp.proto

import com.squareup.wire.FieldEncoding
import com.squareup.wire.Message
import com.squareup.wire.ProtoAdapter
import com.squareup.wire.ProtoReader
import com.squareup.wire.ProtoWriter
import com.squareup.wire.Syntax.PROTO_3
import com.squareup.wire.WireField
import com.squareup.wire.internal.sanitize
import kotlin.Any
import kotlin.AssertionError
import kotlin.Boolean
import kotlin.Deprecated
import kotlin.DeprecationLevel
import kotlin.Int
import kotlin.Long
import kotlin.Nothing
import kotlin.String
import kotlin.hashCode
import kotlin.jvm.JvmField
import okio.ByteString

class LoadSessionResponse(
  @field:WireField(
    tag = 1,
    adapter = "com.squareup.wire.ProtoAdapter#STRING",
    label = WireField.Label.OMIT_IDENTITY,
    jsonName = "sessionId"
  )
  val session_id: String = "",
  @field:WireField(
    tag = 2,
    adapter = "gg.strims.ppspp.proto.Profile#ADAPTER",
    label = WireField.Label.OMIT_IDENTITY
  )
  val profile: Profile? = null,
  unknownFields: ByteString = ByteString.EMPTY
) : Message<LoadSessionResponse, Nothing>(ADAPTER, unknownFields) {
  @Deprecated(
    message = "Shouldn't be used in Kotlin",
    level = DeprecationLevel.HIDDEN
  )
  override fun newBuilder(): Nothing = throw AssertionError()

  override fun equals(other: Any?): Boolean {
    if (other === this) return true
    if (other !is LoadSessionResponse) return false
    if (unknownFields != other.unknownFields) return false
    if (session_id != other.session_id) return false
    if (profile != other.profile) return false
    return true
  }

  override fun hashCode(): Int {
    var result = super.hashCode
    if (result == 0) {
      result = unknownFields.hashCode()
      result = result * 37 + session_id.hashCode()
      result = result * 37 + profile.hashCode()
      super.hashCode = result
    }
    return result
  }

  override fun toString(): String {
    val result = mutableListOf<String>()
    result += """session_id=${sanitize(session_id)}"""
    if (profile != null) result += """profile=$profile"""
    return result.joinToString(prefix = "LoadSessionResponse{", separator = ", ", postfix = "}")
  }

  fun copy(
    session_id: String = this.session_id,
    profile: Profile? = this.profile,
    unknownFields: ByteString = this.unknownFields
  ): LoadSessionResponse = LoadSessionResponse(session_id, profile, unknownFields)

  companion object {
    @JvmField
    val ADAPTER: ProtoAdapter<LoadSessionResponse> = object : ProtoAdapter<LoadSessionResponse>(
      FieldEncoding.LENGTH_DELIMITED, 
      LoadSessionResponse::class, 
      "type.googleapis.com/LoadSessionResponse", 
      PROTO_3, 
      null
    ) {
      override fun encodedSize(value: LoadSessionResponse): Int {
        var size = value.unknownFields.size
        if (value.session_id != "") size += ProtoAdapter.STRING.encodedSizeWithTag(1,
            value.session_id)
        if (value.profile != null) size += Profile.ADAPTER.encodedSizeWithTag(2, value.profile)
        return size
      }

      override fun encode(writer: ProtoWriter, value: LoadSessionResponse) {
        if (value.session_id != "") ProtoAdapter.STRING.encodeWithTag(writer, 1, value.session_id)
        if (value.profile != null) Profile.ADAPTER.encodeWithTag(writer, 2, value.profile)
        writer.writeBytes(value.unknownFields)
      }

      override fun decode(reader: ProtoReader): LoadSessionResponse {
        var session_id: String = ""
        var profile: Profile? = null
        val unknownFields = reader.forEachTag { tag ->
          when (tag) {
            1 -> session_id = ProtoAdapter.STRING.decode(reader)
            2 -> profile = Profile.ADAPTER.decode(reader)
            else -> reader.readUnknownField(tag)
          }
        }
        return LoadSessionResponse(
          session_id = session_id,
          profile = profile,
          unknownFields = unknownFields
        )
      }

      override fun redact(value: LoadSessionResponse): LoadSessionResponse = value.copy(
        profile = value.profile?.let(Profile.ADAPTER::redact),
        unknownFields = ByteString.EMPTY
      )
    }

    private const val serialVersionUID: Long = 0L
  }
}
