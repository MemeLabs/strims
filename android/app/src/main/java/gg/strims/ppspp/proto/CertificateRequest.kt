// Code generated by Wire protocol buffer compiler, do not edit.
// Source: CertificateRequest in profile.proto
package gg.strims.ppspp.proto

import com.squareup.wire.FieldEncoding
import com.squareup.wire.Message
import com.squareup.wire.ProtoAdapter
import com.squareup.wire.ProtoReader
import com.squareup.wire.ProtoWriter
import com.squareup.wire.Syntax.PROTO_3
import com.squareup.wire.WireField
import com.squareup.wire.internal.sanitize
import kotlin.Any
import kotlin.AssertionError
import kotlin.Boolean
import kotlin.Deprecated
import kotlin.DeprecationLevel
import kotlin.Int
import kotlin.Long
import kotlin.Nothing
import kotlin.String
import kotlin.hashCode
import kotlin.jvm.JvmField
import okio.ByteString

class CertificateRequest(
  @field:WireField(
    tag = 1,
    adapter = "com.squareup.wire.ProtoAdapter#BYTES",
    label = WireField.Label.OMIT_IDENTITY
  )
  val key: ByteString = ByteString.EMPTY,
  @field:WireField(
    tag = 2,
    adapter = "gg.strims.ppspp.proto.KeyType#ADAPTER",
    label = WireField.Label.OMIT_IDENTITY,
    jsonName = "keyType"
  )
  val key_type: KeyType = KeyType.KEY_TYPE_UNDEFINED,
  @field:WireField(
    tag = 3,
    adapter = "com.squareup.wire.ProtoAdapter#UINT32",
    label = WireField.Label.OMIT_IDENTITY,
    jsonName = "keyUsage"
  )
  val key_usage: Int = 0,
  @field:WireField(
    tag = 5,
    adapter = "com.squareup.wire.ProtoAdapter#STRING",
    label = WireField.Label.OMIT_IDENTITY
  )
  val subject: String = "",
  @field:WireField(
    tag = 4,
    adapter = "com.squareup.wire.ProtoAdapter#BYTES",
    label = WireField.Label.OMIT_IDENTITY
  )
  val signature: ByteString = ByteString.EMPTY,
  unknownFields: ByteString = ByteString.EMPTY
) : Message<CertificateRequest, Nothing>(ADAPTER, unknownFields) {
  @Deprecated(
    message = "Shouldn't be used in Kotlin",
    level = DeprecationLevel.HIDDEN
  )
  override fun newBuilder(): Nothing = throw AssertionError()

  override fun equals(other: Any?): Boolean {
    if (other === this) return true
    if (other !is CertificateRequest) return false
    if (unknownFields != other.unknownFields) return false
    if (key != other.key) return false
    if (key_type != other.key_type) return false
    if (key_usage != other.key_usage) return false
    if (subject != other.subject) return false
    if (signature != other.signature) return false
    return true
  }

  override fun hashCode(): Int {
    var result = super.hashCode
    if (result == 0) {
      result = unknownFields.hashCode()
      result = result * 37 + key.hashCode()
      result = result * 37 + key_type.hashCode()
      result = result * 37 + key_usage.hashCode()
      result = result * 37 + subject.hashCode()
      result = result * 37 + signature.hashCode()
      super.hashCode = result
    }
    return result
  }

  override fun toString(): String {
    val result = mutableListOf<String>()
    result += """key=$key"""
    result += """key_type=$key_type"""
    result += """key_usage=$key_usage"""
    result += """subject=${sanitize(subject)}"""
    result += """signature=$signature"""
    return result.joinToString(prefix = "CertificateRequest{", separator = ", ", postfix = "}")
  }

  fun copy(
    key: ByteString = this.key,
    key_type: KeyType = this.key_type,
    key_usage: Int = this.key_usage,
    subject: String = this.subject,
    signature: ByteString = this.signature,
    unknownFields: ByteString = this.unknownFields
  ): CertificateRequest = CertificateRequest(key, key_type, key_usage, subject, signature,
      unknownFields)

  companion object {
    @JvmField
    val ADAPTER: ProtoAdapter<CertificateRequest> = object : ProtoAdapter<CertificateRequest>(
      FieldEncoding.LENGTH_DELIMITED, 
      CertificateRequest::class, 
      "type.googleapis.com/CertificateRequest", 
      PROTO_3, 
      null
    ) {
      override fun encodedSize(value: CertificateRequest): Int {
        var size = value.unknownFields.size
        if (value.key != ByteString.EMPTY) size += ProtoAdapter.BYTES.encodedSizeWithTag(1,
            value.key)
        if (value.key_type != KeyType.KEY_TYPE_UNDEFINED) size +=
            KeyType.ADAPTER.encodedSizeWithTag(2, value.key_type)
        if (value.key_usage != 0) size += ProtoAdapter.UINT32.encodedSizeWithTag(3, value.key_usage)
        if (value.subject != "") size += ProtoAdapter.STRING.encodedSizeWithTag(5, value.subject)
        if (value.signature != ByteString.EMPTY) size += ProtoAdapter.BYTES.encodedSizeWithTag(4,
            value.signature)
        return size
      }

      override fun encode(writer: ProtoWriter, value: CertificateRequest) {
        if (value.key != ByteString.EMPTY) ProtoAdapter.BYTES.encodeWithTag(writer, 1, value.key)
        if (value.key_type != KeyType.KEY_TYPE_UNDEFINED) KeyType.ADAPTER.encodeWithTag(writer, 2,
            value.key_type)
        if (value.key_usage != 0) ProtoAdapter.UINT32.encodeWithTag(writer, 3, value.key_usage)
        if (value.subject != "") ProtoAdapter.STRING.encodeWithTag(writer, 5, value.subject)
        if (value.signature != ByteString.EMPTY) ProtoAdapter.BYTES.encodeWithTag(writer, 4,
            value.signature)
        writer.writeBytes(value.unknownFields)
      }

      override fun decode(reader: ProtoReader): CertificateRequest {
        var key: ByteString = ByteString.EMPTY
        var key_type: KeyType = KeyType.KEY_TYPE_UNDEFINED
        var key_usage: Int = 0
        var subject: String = ""
        var signature: ByteString = ByteString.EMPTY
        val unknownFields = reader.forEachTag { tag ->
          when (tag) {
            1 -> key = ProtoAdapter.BYTES.decode(reader)
            2 -> try {
              key_type = KeyType.ADAPTER.decode(reader)
            } catch (e: ProtoAdapter.EnumConstantNotFoundException) {
              reader.addUnknownField(tag, FieldEncoding.VARINT, e.value.toLong())
            }
            3 -> key_usage = ProtoAdapter.UINT32.decode(reader)
            5 -> subject = ProtoAdapter.STRING.decode(reader)
            4 -> signature = ProtoAdapter.BYTES.decode(reader)
            else -> reader.readUnknownField(tag)
          }
        }
        return CertificateRequest(
          key = key,
          key_type = key_type,
          key_usage = key_usage,
          subject = subject,
          signature = signature,
          unknownFields = unknownFields
        )
      }

      override fun redact(value: CertificateRequest): CertificateRequest = value.copy(
        unknownFields = ByteString.EMPTY
      )
    }

    private const val serialVersionUID: Long = 0L
  }
}
