// Code generated by Wire protocol buffer compiler, do not edit.
// Source: ServerConfig in nickserv.proto
package gg.strims.ppspp.proto

import com.squareup.wire.Duration
import com.squareup.wire.FieldEncoding
import com.squareup.wire.Message
import com.squareup.wire.ProtoAdapter
import com.squareup.wire.ProtoReader
import com.squareup.wire.ProtoWriter
import com.squareup.wire.Syntax.PROTO_3
import com.squareup.wire.WireField
import com.squareup.wire.internal.immutableCopyOf
import com.squareup.wire.internal.sanitize
import kotlin.Any
import kotlin.AssertionError
import kotlin.Boolean
import kotlin.Deprecated
import kotlin.DeprecationLevel
import kotlin.Int
import kotlin.Long
import kotlin.Nothing
import kotlin.String
import kotlin.collections.List
import kotlin.hashCode
import kotlin.jvm.JvmField
import okio.ByteString

class ServerConfig(
  @field:WireField(
    tag = 1,
    adapter = "gg.strims.ppspp.proto.Key#ADAPTER",
    label = WireField.Label.OMIT_IDENTITY
  )
  val key: Key? = null,
  @field:WireField(
    tag = 2,
    adapter = "com.squareup.wire.ProtoAdapter#UINT32",
    label = WireField.Label.OMIT_IDENTITY,
    jsonName = "nameChangeQuota"
  )
  val name_change_quota: Int = 0,
  @field:WireField(
    tag = 3,
    adapter = "com.squareup.wire.ProtoAdapter#DURATION",
    label = WireField.Label.OMIT_IDENTITY,
    jsonName = "tokenTtl"
  )
  val token_ttl: Duration? = null,
  roles: List<String> = emptyList(),
  unknownFields: ByteString = ByteString.EMPTY
) : Message<ServerConfig, Nothing>(ADAPTER, unknownFields) {
  @field:WireField(
    tag = 4,
    adapter = "com.squareup.wire.ProtoAdapter#STRING",
    label = WireField.Label.REPEATED
  )
  val roles: List<String> = immutableCopyOf("roles", roles)

  @Deprecated(
    message = "Shouldn't be used in Kotlin",
    level = DeprecationLevel.HIDDEN
  )
  override fun newBuilder(): Nothing = throw AssertionError()

  override fun equals(other: Any?): Boolean {
    if (other === this) return true
    if (other !is ServerConfig) return false
    if (unknownFields != other.unknownFields) return false
    if (key != other.key) return false
    if (name_change_quota != other.name_change_quota) return false
    if (token_ttl != other.token_ttl) return false
    if (roles != other.roles) return false
    return true
  }

  override fun hashCode(): Int {
    var result = super.hashCode
    if (result == 0) {
      result = unknownFields.hashCode()
      result = result * 37 + key.hashCode()
      result = result * 37 + name_change_quota.hashCode()
      result = result * 37 + token_ttl.hashCode()
      result = result * 37 + roles.hashCode()
      super.hashCode = result
    }
    return result
  }

  override fun toString(): String {
    val result = mutableListOf<String>()
    if (key != null) result += """key=$key"""
    result += """name_change_quota=$name_change_quota"""
    if (token_ttl != null) result += """token_ttl=$token_ttl"""
    if (roles.isNotEmpty()) result += """roles=${sanitize(roles)}"""
    return result.joinToString(prefix = "ServerConfig{", separator = ", ", postfix = "}")
  }

  fun copy(
    key: Key? = this.key,
    name_change_quota: Int = this.name_change_quota,
    token_ttl: Duration? = this.token_ttl,
    roles: List<String> = this.roles,
    unknownFields: ByteString = this.unknownFields
  ): ServerConfig = ServerConfig(key, name_change_quota, token_ttl, roles, unknownFields)

  companion object {
    @JvmField
    val ADAPTER: ProtoAdapter<ServerConfig> = object : ProtoAdapter<ServerConfig>(
      FieldEncoding.LENGTH_DELIMITED, 
      ServerConfig::class, 
      "type.googleapis.com/ServerConfig", 
      PROTO_3, 
      null
    ) {
      override fun encodedSize(value: ServerConfig): Int {
        var size = value.unknownFields.size
        if (value.key != null) size += Key.ADAPTER.encodedSizeWithTag(1, value.key)
        if (value.name_change_quota != 0) size += ProtoAdapter.UINT32.encodedSizeWithTag(2,
            value.name_change_quota)
        if (value.token_ttl != null) size += ProtoAdapter.DURATION.encodedSizeWithTag(3,
            value.token_ttl)
        size += ProtoAdapter.STRING.asRepeated().encodedSizeWithTag(4, value.roles)
        return size
      }

      override fun encode(writer: ProtoWriter, value: ServerConfig) {
        if (value.key != null) Key.ADAPTER.encodeWithTag(writer, 1, value.key)
        if (value.name_change_quota != 0) ProtoAdapter.UINT32.encodeWithTag(writer, 2,
            value.name_change_quota)
        if (value.token_ttl != null) ProtoAdapter.DURATION.encodeWithTag(writer, 3, value.token_ttl)
        ProtoAdapter.STRING.asRepeated().encodeWithTag(writer, 4, value.roles)
        writer.writeBytes(value.unknownFields)
      }

      override fun decode(reader: ProtoReader): ServerConfig {
        var key: Key? = null
        var name_change_quota: Int = 0
        var token_ttl: Duration? = null
        val roles = mutableListOf<String>()
        val unknownFields = reader.forEachTag { tag ->
          when (tag) {
            1 -> key = Key.ADAPTER.decode(reader)
            2 -> name_change_quota = ProtoAdapter.UINT32.decode(reader)
            3 -> token_ttl = ProtoAdapter.DURATION.decode(reader)
            4 -> roles.add(ProtoAdapter.STRING.decode(reader))
            else -> reader.readUnknownField(tag)
          }
        }
        return ServerConfig(
          key = key,
          name_change_quota = name_change_quota,
          token_ttl = token_ttl,
          roles = roles,
          unknownFields = unknownFields
        )
      }

      override fun redact(value: ServerConfig): ServerConfig = value.copy(
        key = value.key?.let(Key.ADAPTER::redact),
        token_ttl = value.token_ttl?.let(ProtoAdapter.DURATION::redact),
        unknownFields = ByteString.EMPTY
      )
    }

    private const val serialVersionUID: Long = 0L
  }
}
