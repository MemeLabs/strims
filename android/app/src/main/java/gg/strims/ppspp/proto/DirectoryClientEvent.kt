// Code generated by Wire protocol buffer compiler, do not edit.
// Source: DirectoryClientEvent in directory.proto
package gg.strims.ppspp.proto

import com.squareup.wire.FieldEncoding
import com.squareup.wire.Message
import com.squareup.wire.ProtoAdapter
import com.squareup.wire.ProtoReader
import com.squareup.wire.ProtoWriter
import com.squareup.wire.Syntax
import com.squareup.wire.Syntax.PROTO_3
import com.squareup.wire.WireField
import com.squareup.wire.internal.countNonNull
import kotlin.Any
import kotlin.AssertionError
import kotlin.Boolean
import kotlin.Deprecated
import kotlin.DeprecationLevel
import kotlin.Int
import kotlin.Long
import kotlin.Nothing
import kotlin.String
import kotlin.hashCode
import kotlin.jvm.JvmField
import okio.ByteString

class DirectoryClientEvent(
  @field:WireField(
    tag = 1,
    adapter = "gg.strims.ppspp.proto.DirectoryClientEvent${'$'}Publish#ADAPTER"
  )
  val publish: Publish? = null,
  @field:WireField(
    tag = 2,
    adapter = "gg.strims.ppspp.proto.DirectoryClientEvent${'$'}Unpublish#ADAPTER"
  )
  val unpublish: Unpublish? = null,
  @field:WireField(
    tag = 3,
    adapter = "gg.strims.ppspp.proto.DirectoryClientEvent${'$'}Join#ADAPTER"
  )
  val join: Join? = null,
  @field:WireField(
    tag = 4,
    adapter = "gg.strims.ppspp.proto.DirectoryClientEvent${'$'}Part#ADAPTER"
  )
  val part: Part? = null,
  @field:WireField(
    tag = 5,
    adapter = "gg.strims.ppspp.proto.DirectoryClientEvent${'$'}Ping#ADAPTER"
  )
  val ping: Ping? = null,
  unknownFields: ByteString = ByteString.EMPTY
) : Message<DirectoryClientEvent, Nothing>(ADAPTER, unknownFields) {
  init {
    require(countNonNull(publish, unpublish, join, part, ping) <= 1) {
      "At most one of publish, unpublish, join, part, ping may be non-null"
    }
  }

  @Deprecated(
    message = "Shouldn't be used in Kotlin",
    level = DeprecationLevel.HIDDEN
  )
  override fun newBuilder(): Nothing = throw AssertionError()

  override fun equals(other: Any?): Boolean {
    if (other === this) return true
    if (other !is DirectoryClientEvent) return false
    if (unknownFields != other.unknownFields) return false
    if (publish != other.publish) return false
    if (unpublish != other.unpublish) return false
    if (join != other.join) return false
    if (part != other.part) return false
    if (ping != other.ping) return false
    return true
  }

  override fun hashCode(): Int {
    var result = super.hashCode
    if (result == 0) {
      result = unknownFields.hashCode()
      result = result * 37 + publish.hashCode()
      result = result * 37 + unpublish.hashCode()
      result = result * 37 + join.hashCode()
      result = result * 37 + part.hashCode()
      result = result * 37 + ping.hashCode()
      super.hashCode = result
    }
    return result
  }

  override fun toString(): String {
    val result = mutableListOf<String>()
    if (publish != null) result += """publish=$publish"""
    if (unpublish != null) result += """unpublish=$unpublish"""
    if (join != null) result += """join=$join"""
    if (part != null) result += """part=$part"""
    if (ping != null) result += """ping=$ping"""
    return result.joinToString(prefix = "DirectoryClientEvent{", separator = ", ", postfix = "}")
  }

  fun copy(
    publish: Publish? = this.publish,
    unpublish: Unpublish? = this.unpublish,
    join: Join? = this.join,
    part: Part? = this.part,
    ping: Ping? = this.ping,
    unknownFields: ByteString = this.unknownFields
  ): DirectoryClientEvent = DirectoryClientEvent(publish, unpublish, join, part, ping,
      unknownFields)

  companion object {
    @JvmField
    val ADAPTER: ProtoAdapter<DirectoryClientEvent> = object : ProtoAdapter<DirectoryClientEvent>(
      FieldEncoding.LENGTH_DELIMITED, 
      DirectoryClientEvent::class, 
      "type.googleapis.com/DirectoryClientEvent", 
      PROTO_3, 
      null
    ) {
      override fun encodedSize(value: DirectoryClientEvent): Int {
        var size = value.unknownFields.size
        size += Publish.ADAPTER.encodedSizeWithTag(1, value.publish)
        size += Unpublish.ADAPTER.encodedSizeWithTag(2, value.unpublish)
        size += Join.ADAPTER.encodedSizeWithTag(3, value.join)
        size += Part.ADAPTER.encodedSizeWithTag(4, value.part)
        size += Ping.ADAPTER.encodedSizeWithTag(5, value.ping)
        return size
      }

      override fun encode(writer: ProtoWriter, value: DirectoryClientEvent) {
        Publish.ADAPTER.encodeWithTag(writer, 1, value.publish)
        Unpublish.ADAPTER.encodeWithTag(writer, 2, value.unpublish)
        Join.ADAPTER.encodeWithTag(writer, 3, value.join)
        Part.ADAPTER.encodeWithTag(writer, 4, value.part)
        Ping.ADAPTER.encodeWithTag(writer, 5, value.ping)
        writer.writeBytes(value.unknownFields)
      }

      override fun decode(reader: ProtoReader): DirectoryClientEvent {
        var publish: Publish? = null
        var unpublish: Unpublish? = null
        var join: Join? = null
        var part: Part? = null
        var ping: Ping? = null
        val unknownFields = reader.forEachTag { tag ->
          when (tag) {
            1 -> publish = Publish.ADAPTER.decode(reader)
            2 -> unpublish = Unpublish.ADAPTER.decode(reader)
            3 -> join = Join.ADAPTER.decode(reader)
            4 -> part = Part.ADAPTER.decode(reader)
            5 -> ping = Ping.ADAPTER.decode(reader)
            else -> reader.readUnknownField(tag)
          }
        }
        return DirectoryClientEvent(
          publish = publish,
          unpublish = unpublish,
          join = join,
          part = part,
          ping = ping,
          unknownFields = unknownFields
        )
      }

      override fun redact(value: DirectoryClientEvent): DirectoryClientEvent = value.copy(
        publish = value.publish?.let(Publish.ADAPTER::redact),
        unpublish = value.unpublish?.let(Unpublish.ADAPTER::redact),
        join = value.join?.let(Join.ADAPTER::redact),
        part = value.part?.let(Part.ADAPTER::redact),
        ping = value.ping?.let(Ping.ADAPTER::redact),
        unknownFields = ByteString.EMPTY
      )
    }

    private const val serialVersionUID: Long = 0L
  }

  class Publish(
    @field:WireField(
      tag = 1,
      adapter = "gg.strims.ppspp.proto.DirectoryListing#ADAPTER",
      label = WireField.Label.OMIT_IDENTITY
    )
    val listing: DirectoryListing? = null,
    @field:WireField(
      tag = 2,
      adapter = "com.squareup.wire.ProtoAdapter#BYTES",
      label = WireField.Label.OMIT_IDENTITY
    )
    val signature: ByteString = ByteString.EMPTY,
    unknownFields: ByteString = ByteString.EMPTY
  ) : Message<Publish, Nothing>(ADAPTER, unknownFields) {
    @Deprecated(
      message = "Shouldn't be used in Kotlin",
      level = DeprecationLevel.HIDDEN
    )
    override fun newBuilder(): Nothing = throw AssertionError()

    override fun equals(other: Any?): Boolean {
      if (other === this) return true
      if (other !is Publish) return false
      if (unknownFields != other.unknownFields) return false
      if (listing != other.listing) return false
      if (signature != other.signature) return false
      return true
    }

    override fun hashCode(): Int {
      var result = super.hashCode
      if (result == 0) {
        result = unknownFields.hashCode()
        result = result * 37 + listing.hashCode()
        result = result * 37 + signature.hashCode()
        super.hashCode = result
      }
      return result
    }

    override fun toString(): String {
      val result = mutableListOf<String>()
      if (listing != null) result += """listing=$listing"""
      result += """signature=$signature"""
      return result.joinToString(prefix = "Publish{", separator = ", ", postfix = "}")
    }

    fun copy(
      listing: DirectoryListing? = this.listing,
      signature: ByteString = this.signature,
      unknownFields: ByteString = this.unknownFields
    ): Publish = Publish(listing, signature, unknownFields)

    companion object {
      @JvmField
      val ADAPTER: ProtoAdapter<Publish> = object : ProtoAdapter<Publish>(
        FieldEncoding.LENGTH_DELIMITED, 
        Publish::class, 
        "type.googleapis.com/DirectoryClientEvent.Publish", 
        PROTO_3, 
        null
      ) {
        override fun encodedSize(value: Publish): Int {
          var size = value.unknownFields.size
          if (value.listing != null) size += DirectoryListing.ADAPTER.encodedSizeWithTag(1,
              value.listing)
          if (value.signature != ByteString.EMPTY) size += ProtoAdapter.BYTES.encodedSizeWithTag(2,
              value.signature)
          return size
        }

        override fun encode(writer: ProtoWriter, value: Publish) {
          if (value.listing != null) DirectoryListing.ADAPTER.encodeWithTag(writer, 1,
              value.listing)
          if (value.signature != ByteString.EMPTY) ProtoAdapter.BYTES.encodeWithTag(writer, 2,
              value.signature)
          writer.writeBytes(value.unknownFields)
        }

        override fun decode(reader: ProtoReader): Publish {
          var listing: DirectoryListing? = null
          var signature: ByteString = ByteString.EMPTY
          val unknownFields = reader.forEachTag { tag ->
            when (tag) {
              1 -> listing = DirectoryListing.ADAPTER.decode(reader)
              2 -> signature = ProtoAdapter.BYTES.decode(reader)
              else -> reader.readUnknownField(tag)
            }
          }
          return Publish(
            listing = listing,
            signature = signature,
            unknownFields = unknownFields
          )
        }

        override fun redact(value: Publish): Publish = value.copy(
          listing = value.listing?.let(DirectoryListing.ADAPTER::redact),
          unknownFields = ByteString.EMPTY
        )
      }

      private const val serialVersionUID: Long = 0L
    }
  }

  class Unpublish(
    @field:WireField(
      tag = 1,
      adapter = "com.squareup.wire.ProtoAdapter#BYTES",
      label = WireField.Label.OMIT_IDENTITY
    )
    val key: ByteString = ByteString.EMPTY,
    unknownFields: ByteString = ByteString.EMPTY
  ) : Message<Unpublish, Nothing>(ADAPTER, unknownFields) {
    @Deprecated(
      message = "Shouldn't be used in Kotlin",
      level = DeprecationLevel.HIDDEN
    )
    override fun newBuilder(): Nothing = throw AssertionError()

    override fun equals(other: Any?): Boolean {
      if (other === this) return true
      if (other !is Unpublish) return false
      if (unknownFields != other.unknownFields) return false
      if (key != other.key) return false
      return true
    }

    override fun hashCode(): Int {
      var result = super.hashCode
      if (result == 0) {
        result = unknownFields.hashCode()
        result = result * 37 + key.hashCode()
        super.hashCode = result
      }
      return result
    }

    override fun toString(): String {
      val result = mutableListOf<String>()
      result += """key=$key"""
      return result.joinToString(prefix = "Unpublish{", separator = ", ", postfix = "}")
    }

    fun copy(key: ByteString = this.key, unknownFields: ByteString = this.unknownFields): Unpublish
        = Unpublish(key, unknownFields)

    companion object {
      @JvmField
      val ADAPTER: ProtoAdapter<Unpublish> = object : ProtoAdapter<Unpublish>(
        FieldEncoding.LENGTH_DELIMITED, 
        Unpublish::class, 
        "type.googleapis.com/DirectoryClientEvent.Unpublish", 
        PROTO_3, 
        null
      ) {
        override fun encodedSize(value: Unpublish): Int {
          var size = value.unknownFields.size
          if (value.key != ByteString.EMPTY) size += ProtoAdapter.BYTES.encodedSizeWithTag(1,
              value.key)
          return size
        }

        override fun encode(writer: ProtoWriter, value: Unpublish) {
          if (value.key != ByteString.EMPTY) ProtoAdapter.BYTES.encodeWithTag(writer, 1, value.key)
          writer.writeBytes(value.unknownFields)
        }

        override fun decode(reader: ProtoReader): Unpublish {
          var key: ByteString = ByteString.EMPTY
          val unknownFields = reader.forEachTag { tag ->
            when (tag) {
              1 -> key = ProtoAdapter.BYTES.decode(reader)
              else -> reader.readUnknownField(tag)
            }
          }
          return Unpublish(
            key = key,
            unknownFields = unknownFields
          )
        }

        override fun redact(value: Unpublish): Unpublish = value.copy(
          unknownFields = ByteString.EMPTY
        )
      }

      private const val serialVersionUID: Long = 0L
    }
  }

  class Join(
    @field:WireField(
      tag = 1,
      adapter = "com.squareup.wire.ProtoAdapter#BYTES",
      label = WireField.Label.OMIT_IDENTITY
    )
    val key: ByteString = ByteString.EMPTY,
    unknownFields: ByteString = ByteString.EMPTY
  ) : Message<Join, Nothing>(ADAPTER, unknownFields) {
    @Deprecated(
      message = "Shouldn't be used in Kotlin",
      level = DeprecationLevel.HIDDEN
    )
    override fun newBuilder(): Nothing = throw AssertionError()

    override fun equals(other: Any?): Boolean {
      if (other === this) return true
      if (other !is Join) return false
      if (unknownFields != other.unknownFields) return false
      if (key != other.key) return false
      return true
    }

    override fun hashCode(): Int {
      var result = super.hashCode
      if (result == 0) {
        result = unknownFields.hashCode()
        result = result * 37 + key.hashCode()
        super.hashCode = result
      }
      return result
    }

    override fun toString(): String {
      val result = mutableListOf<String>()
      result += """key=$key"""
      return result.joinToString(prefix = "Join{", separator = ", ", postfix = "}")
    }

    fun copy(key: ByteString = this.key, unknownFields: ByteString = this.unknownFields): Join =
        Join(key, unknownFields)

    companion object {
      @JvmField
      val ADAPTER: ProtoAdapter<Join> = object : ProtoAdapter<Join>(
        FieldEncoding.LENGTH_DELIMITED, 
        Join::class, 
        "type.googleapis.com/DirectoryClientEvent.Join", 
        PROTO_3, 
        null
      ) {
        override fun encodedSize(value: Join): Int {
          var size = value.unknownFields.size
          if (value.key != ByteString.EMPTY) size += ProtoAdapter.BYTES.encodedSizeWithTag(1,
              value.key)
          return size
        }

        override fun encode(writer: ProtoWriter, value: Join) {
          if (value.key != ByteString.EMPTY) ProtoAdapter.BYTES.encodeWithTag(writer, 1, value.key)
          writer.writeBytes(value.unknownFields)
        }

        override fun decode(reader: ProtoReader): Join {
          var key: ByteString = ByteString.EMPTY
          val unknownFields = reader.forEachTag { tag ->
            when (tag) {
              1 -> key = ProtoAdapter.BYTES.decode(reader)
              else -> reader.readUnknownField(tag)
            }
          }
          return Join(
            key = key,
            unknownFields = unknownFields
          )
        }

        override fun redact(value: Join): Join = value.copy(
          unknownFields = ByteString.EMPTY
        )
      }

      private const val serialVersionUID: Long = 0L
    }
  }

  class Part(
    @field:WireField(
      tag = 1,
      adapter = "com.squareup.wire.ProtoAdapter#BYTES",
      label = WireField.Label.OMIT_IDENTITY
    )
    val key: ByteString = ByteString.EMPTY,
    unknownFields: ByteString = ByteString.EMPTY
  ) : Message<Part, Nothing>(ADAPTER, unknownFields) {
    @Deprecated(
      message = "Shouldn't be used in Kotlin",
      level = DeprecationLevel.HIDDEN
    )
    override fun newBuilder(): Nothing = throw AssertionError()

    override fun equals(other: Any?): Boolean {
      if (other === this) return true
      if (other !is Part) return false
      if (unknownFields != other.unknownFields) return false
      if (key != other.key) return false
      return true
    }

    override fun hashCode(): Int {
      var result = super.hashCode
      if (result == 0) {
        result = unknownFields.hashCode()
        result = result * 37 + key.hashCode()
        super.hashCode = result
      }
      return result
    }

    override fun toString(): String {
      val result = mutableListOf<String>()
      result += """key=$key"""
      return result.joinToString(prefix = "Part{", separator = ", ", postfix = "}")
    }

    fun copy(key: ByteString = this.key, unknownFields: ByteString = this.unknownFields): Part =
        Part(key, unknownFields)

    companion object {
      @JvmField
      val ADAPTER: ProtoAdapter<Part> = object : ProtoAdapter<Part>(
        FieldEncoding.LENGTH_DELIMITED, 
        Part::class, 
        "type.googleapis.com/DirectoryClientEvent.Part", 
        PROTO_3, 
        null
      ) {
        override fun encodedSize(value: Part): Int {
          var size = value.unknownFields.size
          if (value.key != ByteString.EMPTY) size += ProtoAdapter.BYTES.encodedSizeWithTag(1,
              value.key)
          return size
        }

        override fun encode(writer: ProtoWriter, value: Part) {
          if (value.key != ByteString.EMPTY) ProtoAdapter.BYTES.encodeWithTag(writer, 1, value.key)
          writer.writeBytes(value.unknownFields)
        }

        override fun decode(reader: ProtoReader): Part {
          var key: ByteString = ByteString.EMPTY
          val unknownFields = reader.forEachTag { tag ->
            when (tag) {
              1 -> key = ProtoAdapter.BYTES.decode(reader)
              else -> reader.readUnknownField(tag)
            }
          }
          return Part(
            key = key,
            unknownFields = unknownFields
          )
        }

        override fun redact(value: Part): Part = value.copy(
          unknownFields = ByteString.EMPTY
        )
      }

      private const val serialVersionUID: Long = 0L
    }
  }

  class Ping(
    @field:WireField(
      tag = 1,
      adapter = "com.squareup.wire.ProtoAdapter#INT64",
      label = WireField.Label.OMIT_IDENTITY
    )
    val time: Long = 0L,
    unknownFields: ByteString = ByteString.EMPTY
  ) : Message<Ping, Nothing>(ADAPTER, unknownFields) {
    @Deprecated(
      message = "Shouldn't be used in Kotlin",
      level = DeprecationLevel.HIDDEN
    )
    override fun newBuilder(): Nothing = throw AssertionError()

    override fun equals(other: Any?): Boolean {
      if (other === this) return true
      if (other !is Ping) return false
      if (unknownFields != other.unknownFields) return false
      if (time != other.time) return false
      return true
    }

    override fun hashCode(): Int {
      var result = super.hashCode
      if (result == 0) {
        result = unknownFields.hashCode()
        result = result * 37 + time.hashCode()
        super.hashCode = result
      }
      return result
    }

    override fun toString(): String {
      val result = mutableListOf<String>()
      result += """time=$time"""
      return result.joinToString(prefix = "Ping{", separator = ", ", postfix = "}")
    }

    fun copy(time: Long = this.time, unknownFields: ByteString = this.unknownFields): Ping =
        Ping(time, unknownFields)

    companion object {
      @JvmField
      val ADAPTER: ProtoAdapter<Ping> = object : ProtoAdapter<Ping>(
        FieldEncoding.LENGTH_DELIMITED, 
        Ping::class, 
        "type.googleapis.com/DirectoryClientEvent.Ping", 
        PROTO_3, 
        null
      ) {
        override fun encodedSize(value: Ping): Int {
          var size = value.unknownFields.size
          if (value.time != 0L) size += ProtoAdapter.INT64.encodedSizeWithTag(1, value.time)
          return size
        }

        override fun encode(writer: ProtoWriter, value: Ping) {
          if (value.time != 0L) ProtoAdapter.INT64.encodeWithTag(writer, 1, value.time)
          writer.writeBytes(value.unknownFields)
        }

        override fun decode(reader: ProtoReader): Ping {
          var time: Long = 0L
          val unknownFields = reader.forEachTag { tag ->
            when (tag) {
              1 -> time = ProtoAdapter.INT64.decode(reader)
              else -> reader.readUnknownField(tag)
            }
          }
          return Ping(
            time = time,
            unknownFields = unknownFields
          )
        }

        override fun redact(value: Ping): Ping = value.copy(
          unknownFields = ByteString.EMPTY
        )
      }

      private const val serialVersionUID: Long = 0L
    }
  }
}
