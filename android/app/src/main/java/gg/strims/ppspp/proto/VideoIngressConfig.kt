// Code generated by Wire protocol buffer compiler, do not edit.
// Source: VideoIngressConfig in video.proto
package gg.strims.ppspp.proto

import com.squareup.wire.FieldEncoding
import com.squareup.wire.Message
import com.squareup.wire.ProtoAdapter
import com.squareup.wire.ProtoReader
import com.squareup.wire.ProtoWriter
import com.squareup.wire.Syntax.PROTO_3
import com.squareup.wire.WireField
import com.squareup.wire.internal.immutableCopyOf
import com.squareup.wire.internal.sanitize
import kotlin.Any
import kotlin.AssertionError
import kotlin.Boolean
import kotlin.Deprecated
import kotlin.DeprecationLevel
import kotlin.Int
import kotlin.Long
import kotlin.Nothing
import kotlin.String
import kotlin.collections.List
import kotlin.hashCode
import kotlin.jvm.JvmField
import okio.ByteString

class VideoIngressConfig(
  @field:WireField(
    tag = 1,
    adapter = "com.squareup.wire.ProtoAdapter#BOOL",
    label = WireField.Label.OMIT_IDENTITY
  )
  val enabled: Boolean = false,
  @field:WireField(
    tag = 2,
    adapter = "com.squareup.wire.ProtoAdapter#STRING",
    label = WireField.Label.OMIT_IDENTITY,
    jsonName = "serverAddr"
  )
  val server_addr: String = "",
  @field:WireField(
    tag = 3,
    adapter = "com.squareup.wire.ProtoAdapter#STRING",
    label = WireField.Label.OMIT_IDENTITY,
    jsonName = "publicServerAddr"
  )
  val public_server_addr: String = "",
  service_network_keys: List<ByteString> = emptyList(),
  unknownFields: ByteString = ByteString.EMPTY
) : Message<VideoIngressConfig, Nothing>(ADAPTER, unknownFields) {
  @field:WireField(
    tag = 4,
    adapter = "com.squareup.wire.ProtoAdapter#BYTES",
    label = WireField.Label.REPEATED,
    jsonName = "serviceNetworkKeys"
  )
  val service_network_keys: List<ByteString> = immutableCopyOf("service_network_keys",
      service_network_keys)

  @Deprecated(
    message = "Shouldn't be used in Kotlin",
    level = DeprecationLevel.HIDDEN
  )
  override fun newBuilder(): Nothing = throw AssertionError()

  override fun equals(other: Any?): Boolean {
    if (other === this) return true
    if (other !is VideoIngressConfig) return false
    if (unknownFields != other.unknownFields) return false
    if (enabled != other.enabled) return false
    if (server_addr != other.server_addr) return false
    if (public_server_addr != other.public_server_addr) return false
    if (service_network_keys != other.service_network_keys) return false
    return true
  }

  override fun hashCode(): Int {
    var result = super.hashCode
    if (result == 0) {
      result = unknownFields.hashCode()
      result = result * 37 + enabled.hashCode()
      result = result * 37 + server_addr.hashCode()
      result = result * 37 + public_server_addr.hashCode()
      result = result * 37 + service_network_keys.hashCode()
      super.hashCode = result
    }
    return result
  }

  override fun toString(): String {
    val result = mutableListOf<String>()
    result += """enabled=$enabled"""
    result += """server_addr=${sanitize(server_addr)}"""
    result += """public_server_addr=${sanitize(public_server_addr)}"""
    if (service_network_keys.isNotEmpty()) result +=
        """service_network_keys=$service_network_keys"""
    return result.joinToString(prefix = "VideoIngressConfig{", separator = ", ", postfix = "}")
  }

  fun copy(
    enabled: Boolean = this.enabled,
    server_addr: String = this.server_addr,
    public_server_addr: String = this.public_server_addr,
    service_network_keys: List<ByteString> = this.service_network_keys,
    unknownFields: ByteString = this.unknownFields
  ): VideoIngressConfig = VideoIngressConfig(enabled, server_addr, public_server_addr,
      service_network_keys, unknownFields)

  companion object {
    @JvmField
    val ADAPTER: ProtoAdapter<VideoIngressConfig> = object : ProtoAdapter<VideoIngressConfig>(
      FieldEncoding.LENGTH_DELIMITED, 
      VideoIngressConfig::class, 
      "type.googleapis.com/VideoIngressConfig", 
      PROTO_3, 
      null
    ) {
      override fun encodedSize(value: VideoIngressConfig): Int {
        var size = value.unknownFields.size
        if (value.enabled != false) size += ProtoAdapter.BOOL.encodedSizeWithTag(1, value.enabled)
        if (value.server_addr != "") size += ProtoAdapter.STRING.encodedSizeWithTag(2,
            value.server_addr)
        if (value.public_server_addr != "") size += ProtoAdapter.STRING.encodedSizeWithTag(3,
            value.public_server_addr)
        size += ProtoAdapter.BYTES.asRepeated().encodedSizeWithTag(4, value.service_network_keys)
        return size
      }

      override fun encode(writer: ProtoWriter, value: VideoIngressConfig) {
        if (value.enabled != false) ProtoAdapter.BOOL.encodeWithTag(writer, 1, value.enabled)
        if (value.server_addr != "") ProtoAdapter.STRING.encodeWithTag(writer, 2, value.server_addr)
        if (value.public_server_addr != "") ProtoAdapter.STRING.encodeWithTag(writer, 3,
            value.public_server_addr)
        ProtoAdapter.BYTES.asRepeated().encodeWithTag(writer, 4, value.service_network_keys)
        writer.writeBytes(value.unknownFields)
      }

      override fun decode(reader: ProtoReader): VideoIngressConfig {
        var enabled: Boolean = false
        var server_addr: String = ""
        var public_server_addr: String = ""
        val service_network_keys = mutableListOf<ByteString>()
        val unknownFields = reader.forEachTag { tag ->
          when (tag) {
            1 -> enabled = ProtoAdapter.BOOL.decode(reader)
            2 -> server_addr = ProtoAdapter.STRING.decode(reader)
            3 -> public_server_addr = ProtoAdapter.STRING.decode(reader)
            4 -> service_network_keys.add(ProtoAdapter.BYTES.decode(reader))
            else -> reader.readUnknownField(tag)
          }
        }
        return VideoIngressConfig(
          enabled = enabled,
          server_addr = server_addr,
          public_server_addr = public_server_addr,
          service_network_keys = service_network_keys,
          unknownFields = unknownFields
        )
      }

      override fun redact(value: VideoIngressConfig): VideoIngressConfig = value.copy(
        unknownFields = ByteString.EMPTY
      )
    }

    private const val serialVersionUID: Long = 0L
  }
}
