// Code generated by SQLBoiler 4.11.0 (https://github.com/volatiletech/sqlboiler). DO NOT EDIT.
// This file is meant to be re-generated in place and/or deleted at any time.

package models

import (
	"context"
	"database/sql"
	"fmt"
	"reflect"
	"strconv"
	"strings"
	"sync"
	"time"

	"github.com/friendsofgo/errors"
	"github.com/volatiletech/sqlboiler/v4/boil"
	"github.com/volatiletech/sqlboiler/v4/queries"
	"github.com/volatiletech/sqlboiler/v4/queries/qm"
	"github.com/volatiletech/sqlboiler/v4/queries/qmhelper"
	"github.com/volatiletech/strmangle"
)

// ExternalPeer is an object representing the database table.
type ExternalPeer struct {
	ID                  int64  `boil:"id" json:"id" toml:"id" yaml:"id"`
	Comment             string `boil:"comment" json:"comment" toml:"comment" yaml:"comment"`
	PublicIPV4          string `boil:"public_ip_v4" json:"publicIPV4" toml:"publicIPV4" yaml:"publicIPV4"`
	WireguardPort       int    `boil:"wireguard_port" json:"wireguardPort" toml:"wireguardPort" yaml:"wireguardPort"`
	WireguardPrivateKey string `boil:"wireguard_private_key" json:"wireguardPrivateKey" toml:"wireguardPrivateKey" yaml:"wireguardPrivateKey"`

	R *externalPeerR `boil:"-" json:"-" toml:"-" yaml:"-"`
	L externalPeerL  `boil:"-" json:"-" toml:"-" yaml:"-"`
}

var ExternalPeerColumns = struct {
	ID                  string
	Comment             string
	PublicIPV4          string
	WireguardPort       string
	WireguardPrivateKey string
}{
	ID:                  "id",
	Comment:             "comment",
	PublicIPV4:          "public_ip_v4",
	WireguardPort:       "wireguard_port",
	WireguardPrivateKey: "wireguard_private_key",
}

var ExternalPeerTableColumns = struct {
	ID                  string
	Comment             string
	PublicIPV4          string
	WireguardPort       string
	WireguardPrivateKey string
}{
	ID:                  "external_peers.id",
	Comment:             "external_peers.comment",
	PublicIPV4:          "external_peers.public_ip_v4",
	WireguardPort:       "external_peers.wireguard_port",
	WireguardPrivateKey: "external_peers.wireguard_private_key",
}

// Generated where

type whereHelperint64 struct{ field string }

func (w whereHelperint64) EQ(x int64) qm.QueryMod  { return qmhelper.Where(w.field, qmhelper.EQ, x) }
func (w whereHelperint64) NEQ(x int64) qm.QueryMod { return qmhelper.Where(w.field, qmhelper.NEQ, x) }
func (w whereHelperint64) LT(x int64) qm.QueryMod  { return qmhelper.Where(w.field, qmhelper.LT, x) }
func (w whereHelperint64) LTE(x int64) qm.QueryMod { return qmhelper.Where(w.field, qmhelper.LTE, x) }
func (w whereHelperint64) GT(x int64) qm.QueryMod  { return qmhelper.Where(w.field, qmhelper.GT, x) }
func (w whereHelperint64) GTE(x int64) qm.QueryMod { return qmhelper.Where(w.field, qmhelper.GTE, x) }
func (w whereHelperint64) IN(slice []int64) qm.QueryMod {
	values := make([]interface{}, 0, len(slice))
	for _, value := range slice {
		values = append(values, value)
	}
	return qm.WhereIn(fmt.Sprintf("%s IN ?", w.field), values...)
}
func (w whereHelperint64) NIN(slice []int64) qm.QueryMod {
	values := make([]interface{}, 0, len(slice))
	for _, value := range slice {
		values = append(values, value)
	}
	return qm.WhereNotIn(fmt.Sprintf("%s NOT IN ?", w.field), values...)
}

type whereHelperstring struct{ field string }

func (w whereHelperstring) EQ(x string) qm.QueryMod  { return qmhelper.Where(w.field, qmhelper.EQ, x) }
func (w whereHelperstring) NEQ(x string) qm.QueryMod { return qmhelper.Where(w.field, qmhelper.NEQ, x) }
func (w whereHelperstring) LT(x string) qm.QueryMod  { return qmhelper.Where(w.field, qmhelper.LT, x) }
func (w whereHelperstring) LTE(x string) qm.QueryMod { return qmhelper.Where(w.field, qmhelper.LTE, x) }
func (w whereHelperstring) GT(x string) qm.QueryMod  { return qmhelper.Where(w.field, qmhelper.GT, x) }
func (w whereHelperstring) GTE(x string) qm.QueryMod { return qmhelper.Where(w.field, qmhelper.GTE, x) }
func (w whereHelperstring) IN(slice []string) qm.QueryMod {
	values := make([]interface{}, 0, len(slice))
	for _, value := range slice {
		values = append(values, value)
	}
	return qm.WhereIn(fmt.Sprintf("%s IN ?", w.field), values...)
}
func (w whereHelperstring) NIN(slice []string) qm.QueryMod {
	values := make([]interface{}, 0, len(slice))
	for _, value := range slice {
		values = append(values, value)
	}
	return qm.WhereNotIn(fmt.Sprintf("%s NOT IN ?", w.field), values...)
}

type whereHelperint struct{ field string }

func (w whereHelperint) EQ(x int) qm.QueryMod  { return qmhelper.Where(w.field, qmhelper.EQ, x) }
func (w whereHelperint) NEQ(x int) qm.QueryMod { return qmhelper.Where(w.field, qmhelper.NEQ, x) }
func (w whereHelperint) LT(x int) qm.QueryMod  { return qmhelper.Where(w.field, qmhelper.LT, x) }
func (w whereHelperint) LTE(x int) qm.QueryMod { return qmhelper.Where(w.field, qmhelper.LTE, x) }
func (w whereHelperint) GT(x int) qm.QueryMod  { return qmhelper.Where(w.field, qmhelper.GT, x) }
func (w whereHelperint) GTE(x int) qm.QueryMod { return qmhelper.Where(w.field, qmhelper.GTE, x) }
func (w whereHelperint) IN(slice []int) qm.QueryMod {
	values := make([]interface{}, 0, len(slice))
	for _, value := range slice {
		values = append(values, value)
	}
	return qm.WhereIn(fmt.Sprintf("%s IN ?", w.field), values...)
}
func (w whereHelperint) NIN(slice []int) qm.QueryMod {
	values := make([]interface{}, 0, len(slice))
	for _, value := range slice {
		values = append(values, value)
	}
	return qm.WhereNotIn(fmt.Sprintf("%s NOT IN ?", w.field), values...)
}

var ExternalPeerWhere = struct {
	ID                  whereHelperint64
	Comment             whereHelperstring
	PublicIPV4          whereHelperstring
	WireguardPort       whereHelperint
	WireguardPrivateKey whereHelperstring
}{
	ID:                  whereHelperint64{field: "\"external_peers\".\"id\""},
	Comment:             whereHelperstring{field: "\"external_peers\".\"comment\""},
	PublicIPV4:          whereHelperstring{field: "\"external_peers\".\"public_ip_v4\""},
	WireguardPort:       whereHelperint{field: "\"external_peers\".\"wireguard_port\""},
	WireguardPrivateKey: whereHelperstring{field: "\"external_peers\".\"wireguard_private_key\""},
}

// ExternalPeerRels is where relationship names are stored.
var ExternalPeerRels = struct {
}{}

// externalPeerR is where relationships are stored.
type externalPeerR struct {
}

// NewStruct creates a new relationship struct
func (*externalPeerR) NewStruct() *externalPeerR {
	return &externalPeerR{}
}

// externalPeerL is where Load methods for each relationship are stored.
type externalPeerL struct{}

var (
	externalPeerAllColumns            = []string{"id", "comment", "public_ip_v4", "wireguard_port", "wireguard_private_key"}
	externalPeerColumnsWithoutDefault = []string{"comment", "public_ip_v4", "wireguard_port", "wireguard_private_key"}
	externalPeerColumnsWithDefault    = []string{"id"}
	externalPeerPrimaryKeyColumns     = []string{"id"}
	externalPeerGeneratedColumns      = []string{}
)

type (
	// ExternalPeerSlice is an alias for a slice of pointers to ExternalPeer.
	// This should almost always be used instead of []ExternalPeer.
	ExternalPeerSlice []*ExternalPeer

	externalPeerQuery struct {
		*queries.Query
	}
)

// Cache for insert, update and upsert
var (
	externalPeerType                 = reflect.TypeOf(&ExternalPeer{})
	externalPeerMapping              = queries.MakeStructMapping(externalPeerType)
	externalPeerPrimaryKeyMapping, _ = queries.BindMapping(externalPeerType, externalPeerMapping, externalPeerPrimaryKeyColumns)
	externalPeerInsertCacheMut       sync.RWMutex
	externalPeerInsertCache          = make(map[string]insertCache)
	externalPeerUpdateCacheMut       sync.RWMutex
	externalPeerUpdateCache          = make(map[string]updateCache)
	externalPeerUpsertCacheMut       sync.RWMutex
	externalPeerUpsertCache          = make(map[string]insertCache)
)

var (
	// Force time package dependency for automated UpdatedAt/CreatedAt.
	_ = time.Second
	// Force qmhelper dependency for where clause generation (which doesn't
	// always happen)
	_ = qmhelper.Where
)

// OneG returns a single externalPeer record from the query using the global executor.
func (q externalPeerQuery) OneG(ctx context.Context) (*ExternalPeer, error) {
	return q.One(ctx, boil.GetContextDB())
}

// One returns a single externalPeer record from the query.
func (q externalPeerQuery) One(ctx context.Context, exec boil.ContextExecutor) (*ExternalPeer, error) {
	o := &ExternalPeer{}

	queries.SetLimit(q.Query, 1)

	err := q.Bind(ctx, exec, o)
	if err != nil {
		if errors.Is(err, sql.ErrNoRows) {
			return nil, sql.ErrNoRows
		}
		return nil, errors.Wrap(err, "models: failed to execute a one query for external_peers")
	}

	return o, nil
}

// AllG returns all ExternalPeer records from the query using the global executor.
func (q externalPeerQuery) AllG(ctx context.Context) (ExternalPeerSlice, error) {
	return q.All(ctx, boil.GetContextDB())
}

// All returns all ExternalPeer records from the query.
func (q externalPeerQuery) All(ctx context.Context, exec boil.ContextExecutor) (ExternalPeerSlice, error) {
	var o []*ExternalPeer

	err := q.Bind(ctx, exec, &o)
	if err != nil {
		return nil, errors.Wrap(err, "models: failed to assign all query results to ExternalPeer slice")
	}

	return o, nil
}

// CountG returns the count of all ExternalPeer records in the query using the global executor
func (q externalPeerQuery) CountG(ctx context.Context) (int64, error) {
	return q.Count(ctx, boil.GetContextDB())
}

// Count returns the count of all ExternalPeer records in the query.
func (q externalPeerQuery) Count(ctx context.Context, exec boil.ContextExecutor) (int64, error) {
	var count int64

	queries.SetSelect(q.Query, nil)
	queries.SetCount(q.Query)

	err := q.Query.QueryRowContext(ctx, exec).Scan(&count)
	if err != nil {
		return 0, errors.Wrap(err, "models: failed to count external_peers rows")
	}

	return count, nil
}

// ExistsG checks if the row exists in the table using the global executor.
func (q externalPeerQuery) ExistsG(ctx context.Context) (bool, error) {
	return q.Exists(ctx, boil.GetContextDB())
}

// Exists checks if the row exists in the table.
func (q externalPeerQuery) Exists(ctx context.Context, exec boil.ContextExecutor) (bool, error) {
	var count int64

	queries.SetSelect(q.Query, nil)
	queries.SetCount(q.Query)
	queries.SetLimit(q.Query, 1)

	err := q.Query.QueryRowContext(ctx, exec).Scan(&count)
	if err != nil {
		return false, errors.Wrap(err, "models: failed to check if external_peers exists")
	}

	return count > 0, nil
}

// ExternalPeers retrieves all the records using an executor.
func ExternalPeers(mods ...qm.QueryMod) externalPeerQuery {
	mods = append(mods, qm.From("\"external_peers\""))
	q := NewQuery(mods...)
	if len(queries.GetSelect(q)) == 0 {
		queries.SetSelect(q, []string{"\"external_peers\".*"})
	}

	return externalPeerQuery{q}
}

// FindExternalPeerG retrieves a single record by ID.
func FindExternalPeerG(ctx context.Context, iD int64, selectCols ...string) (*ExternalPeer, error) {
	return FindExternalPeer(ctx, boil.GetContextDB(), iD, selectCols...)
}

// FindExternalPeer retrieves a single record by ID with an executor.
// If selectCols is empty Find will return all columns.
func FindExternalPeer(ctx context.Context, exec boil.ContextExecutor, iD int64, selectCols ...string) (*ExternalPeer, error) {
	externalPeerObj := &ExternalPeer{}

	sel := "*"
	if len(selectCols) > 0 {
		sel = strings.Join(strmangle.IdentQuoteSlice(dialect.LQ, dialect.RQ, selectCols), ",")
	}
	query := fmt.Sprintf(
		"select %s from \"external_peers\" where \"id\"=$1", sel,
	)

	q := queries.Raw(query, iD)

	err := q.Bind(ctx, exec, externalPeerObj)
	if err != nil {
		if errors.Is(err, sql.ErrNoRows) {
			return nil, sql.ErrNoRows
		}
		return nil, errors.Wrap(err, "models: unable to select from external_peers")
	}

	return externalPeerObj, nil
}

// InsertG a single record. See Insert for whitelist behavior description.
func (o *ExternalPeer) InsertG(ctx context.Context, columns boil.Columns) error {
	return o.Insert(ctx, boil.GetContextDB(), columns)
}

// Insert a single record using an executor.
// See boil.Columns.InsertColumnSet documentation to understand column list inference for inserts.
func (o *ExternalPeer) Insert(ctx context.Context, exec boil.ContextExecutor, columns boil.Columns) error {
	if o == nil {
		return errors.New("models: no external_peers provided for insertion")
	}

	var err error

	nzDefaults := queries.NonZeroDefaultSet(externalPeerColumnsWithDefault, o)

	key := makeCacheKey(columns, nzDefaults)
	externalPeerInsertCacheMut.RLock()
	cache, cached := externalPeerInsertCache[key]
	externalPeerInsertCacheMut.RUnlock()

	if !cached {
		wl, returnColumns := columns.InsertColumnSet(
			externalPeerAllColumns,
			externalPeerColumnsWithDefault,
			externalPeerColumnsWithoutDefault,
			nzDefaults,
		)

		cache.valueMapping, err = queries.BindMapping(externalPeerType, externalPeerMapping, wl)
		if err != nil {
			return err
		}
		cache.retMapping, err = queries.BindMapping(externalPeerType, externalPeerMapping, returnColumns)
		if err != nil {
			return err
		}
		if len(wl) != 0 {
			cache.query = fmt.Sprintf("INSERT INTO \"external_peers\" (\"%s\") %%sVALUES (%s)%%s", strings.Join(wl, "\",\""), strmangle.Placeholders(dialect.UseIndexPlaceholders, len(wl), 1, 1))
		} else {
			cache.query = "INSERT INTO \"external_peers\" %sDEFAULT VALUES%s"
		}

		var queryOutput, queryReturning string

		if len(cache.retMapping) != 0 {
			queryReturning = fmt.Sprintf(" RETURNING \"%s\"", strings.Join(returnColumns, "\",\""))
		}

		cache.query = fmt.Sprintf(cache.query, queryOutput, queryReturning)
	}

	value := reflect.Indirect(reflect.ValueOf(o))
	vals := queries.ValuesFromMapping(value, cache.valueMapping)

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, cache.query)
		fmt.Fprintln(writer, vals)
	}

	if len(cache.retMapping) != 0 {
		err = exec.QueryRowContext(ctx, cache.query, vals...).Scan(queries.PtrsFromMapping(value, cache.retMapping)...)
	} else {
		_, err = exec.ExecContext(ctx, cache.query, vals...)
	}

	if err != nil {
		return errors.Wrap(err, "models: unable to insert into external_peers")
	}

	if !cached {
		externalPeerInsertCacheMut.Lock()
		externalPeerInsertCache[key] = cache
		externalPeerInsertCacheMut.Unlock()
	}

	return nil
}

// UpdateG a single ExternalPeer record using the global executor.
// See Update for more documentation.
func (o *ExternalPeer) UpdateG(ctx context.Context, columns boil.Columns) (int64, error) {
	return o.Update(ctx, boil.GetContextDB(), columns)
}

// Update uses an executor to update the ExternalPeer.
// See boil.Columns.UpdateColumnSet documentation to understand column list inference for updates.
// Update does not automatically update the record in case of default values. Use .Reload() to refresh the records.
func (o *ExternalPeer) Update(ctx context.Context, exec boil.ContextExecutor, columns boil.Columns) (int64, error) {
	var err error
	key := makeCacheKey(columns, nil)
	externalPeerUpdateCacheMut.RLock()
	cache, cached := externalPeerUpdateCache[key]
	externalPeerUpdateCacheMut.RUnlock()

	if !cached {
		wl := columns.UpdateColumnSet(
			externalPeerAllColumns,
			externalPeerPrimaryKeyColumns,
		)

		if !columns.IsWhitelist() {
			wl = strmangle.SetComplement(wl, []string{"created_at"})
		}
		if len(wl) == 0 {
			return 0, errors.New("models: unable to update external_peers, could not build whitelist")
		}

		cache.query = fmt.Sprintf("UPDATE \"external_peers\" SET %s WHERE %s",
			strmangle.SetParamNames("\"", "\"", 1, wl),
			strmangle.WhereClause("\"", "\"", len(wl)+1, externalPeerPrimaryKeyColumns),
		)
		cache.valueMapping, err = queries.BindMapping(externalPeerType, externalPeerMapping, append(wl, externalPeerPrimaryKeyColumns...))
		if err != nil {
			return 0, err
		}
	}

	values := queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(o)), cache.valueMapping)

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, cache.query)
		fmt.Fprintln(writer, values)
	}
	var result sql.Result
	result, err = exec.ExecContext(ctx, cache.query, values...)
	if err != nil {
		return 0, errors.Wrap(err, "models: unable to update external_peers row")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "models: failed to get rows affected by update for external_peers")
	}

	if !cached {
		externalPeerUpdateCacheMut.Lock()
		externalPeerUpdateCache[key] = cache
		externalPeerUpdateCacheMut.Unlock()
	}

	return rowsAff, nil
}

// UpdateAllG updates all rows with the specified column values.
func (q externalPeerQuery) UpdateAllG(ctx context.Context, cols M) (int64, error) {
	return q.UpdateAll(ctx, boil.GetContextDB(), cols)
}

// UpdateAll updates all rows with the specified column values.
func (q externalPeerQuery) UpdateAll(ctx context.Context, exec boil.ContextExecutor, cols M) (int64, error) {
	queries.SetUpdate(q.Query, cols)

	result, err := q.Query.ExecContext(ctx, exec)
	if err != nil {
		return 0, errors.Wrap(err, "models: unable to update all for external_peers")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "models: unable to retrieve rows affected for external_peers")
	}

	return rowsAff, nil
}

// UpdateAllG updates all rows with the specified column values.
func (o ExternalPeerSlice) UpdateAllG(ctx context.Context, cols M) (int64, error) {
	return o.UpdateAll(ctx, boil.GetContextDB(), cols)
}

// UpdateAll updates all rows with the specified column values, using an executor.
func (o ExternalPeerSlice) UpdateAll(ctx context.Context, exec boil.ContextExecutor, cols M) (int64, error) {
	ln := int64(len(o))
	if ln == 0 {
		return 0, nil
	}

	if len(cols) == 0 {
		return 0, errors.New("models: update all requires at least one column argument")
	}

	colNames := make([]string, len(cols))
	args := make([]interface{}, len(cols))

	i := 0
	for name, value := range cols {
		colNames[i] = name
		args[i] = value
		i++
	}

	// Append all of the primary key values for each column
	for _, obj := range o {
		pkeyArgs := queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(obj)), externalPeerPrimaryKeyMapping)
		args = append(args, pkeyArgs...)
	}

	sql := fmt.Sprintf("UPDATE \"external_peers\" SET %s WHERE %s",
		strmangle.SetParamNames("\"", "\"", 1, colNames),
		strmangle.WhereClauseRepeated(string(dialect.LQ), string(dialect.RQ), len(colNames)+1, externalPeerPrimaryKeyColumns, len(o)))

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, sql)
		fmt.Fprintln(writer, args...)
	}
	result, err := exec.ExecContext(ctx, sql, args...)
	if err != nil {
		return 0, errors.Wrap(err, "models: unable to update all in externalPeer slice")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "models: unable to retrieve rows affected all in update all externalPeer")
	}
	return rowsAff, nil
}

// DeleteG deletes a single ExternalPeer record.
// DeleteG will match against the primary key column to find the record to delete.
func (o *ExternalPeer) DeleteG(ctx context.Context) (int64, error) {
	return o.Delete(ctx, boil.GetContextDB())
}

// Delete deletes a single ExternalPeer record with an executor.
// Delete will match against the primary key column to find the record to delete.
func (o *ExternalPeer) Delete(ctx context.Context, exec boil.ContextExecutor) (int64, error) {
	if o == nil {
		return 0, errors.New("models: no ExternalPeer provided for delete")
	}

	args := queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(o)), externalPeerPrimaryKeyMapping)
	sql := "DELETE FROM \"external_peers\" WHERE \"id\"=$1"

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, sql)
		fmt.Fprintln(writer, args...)
	}
	result, err := exec.ExecContext(ctx, sql, args...)
	if err != nil {
		return 0, errors.Wrap(err, "models: unable to delete from external_peers")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "models: failed to get rows affected by delete for external_peers")
	}

	return rowsAff, nil
}

func (q externalPeerQuery) DeleteAllG(ctx context.Context) (int64, error) {
	return q.DeleteAll(ctx, boil.GetContextDB())
}

// DeleteAll deletes all matching rows.
func (q externalPeerQuery) DeleteAll(ctx context.Context, exec boil.ContextExecutor) (int64, error) {
	if q.Query == nil {
		return 0, errors.New("models: no externalPeerQuery provided for delete all")
	}

	queries.SetDelete(q.Query)

	result, err := q.Query.ExecContext(ctx, exec)
	if err != nil {
		return 0, errors.Wrap(err, "models: unable to delete all from external_peers")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "models: failed to get rows affected by deleteall for external_peers")
	}

	return rowsAff, nil
}

// DeleteAllG deletes all rows in the slice.
func (o ExternalPeerSlice) DeleteAllG(ctx context.Context) (int64, error) {
	return o.DeleteAll(ctx, boil.GetContextDB())
}

// DeleteAll deletes all rows in the slice, using an executor.
func (o ExternalPeerSlice) DeleteAll(ctx context.Context, exec boil.ContextExecutor) (int64, error) {
	if len(o) == 0 {
		return 0, nil
	}

	var args []interface{}
	for _, obj := range o {
		pkeyArgs := queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(obj)), externalPeerPrimaryKeyMapping)
		args = append(args, pkeyArgs...)
	}

	sql := "DELETE FROM \"external_peers\" WHERE " +
		strmangle.WhereClauseRepeated(string(dialect.LQ), string(dialect.RQ), 1, externalPeerPrimaryKeyColumns, len(o))

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, sql)
		fmt.Fprintln(writer, args)
	}
	result, err := exec.ExecContext(ctx, sql, args...)
	if err != nil {
		return 0, errors.Wrap(err, "models: unable to delete all from externalPeer slice")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "models: failed to get rows affected by deleteall for external_peers")
	}

	return rowsAff, nil
}

// ReloadG refetches the object from the database using the primary keys.
func (o *ExternalPeer) ReloadG(ctx context.Context) error {
	if o == nil {
		return errors.New("models: no ExternalPeer provided for reload")
	}

	return o.Reload(ctx, boil.GetContextDB())
}

// Reload refetches the object from the database
// using the primary keys with an executor.
func (o *ExternalPeer) Reload(ctx context.Context, exec boil.ContextExecutor) error {
	ret, err := FindExternalPeer(ctx, exec, o.ID)
	if err != nil {
		return err
	}

	*o = *ret
	return nil
}

// ReloadAllG refetches every row with matching primary key column values
// and overwrites the original object slice with the newly updated slice.
func (o *ExternalPeerSlice) ReloadAllG(ctx context.Context) error {
	if o == nil {
		return errors.New("models: empty ExternalPeerSlice provided for reload all")
	}

	return o.ReloadAll(ctx, boil.GetContextDB())
}

// ReloadAll refetches every row with matching primary key column values
// and overwrites the original object slice with the newly updated slice.
func (o *ExternalPeerSlice) ReloadAll(ctx context.Context, exec boil.ContextExecutor) error {
	if o == nil || len(*o) == 0 {
		return nil
	}

	slice := ExternalPeerSlice{}
	var args []interface{}
	for _, obj := range *o {
		pkeyArgs := queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(obj)), externalPeerPrimaryKeyMapping)
		args = append(args, pkeyArgs...)
	}

	sql := "SELECT \"external_peers\".* FROM \"external_peers\" WHERE " +
		strmangle.WhereClauseRepeated(string(dialect.LQ), string(dialect.RQ), 1, externalPeerPrimaryKeyColumns, len(*o))

	q := queries.Raw(sql, args...)

	err := q.Bind(ctx, exec, &slice)
	if err != nil {
		return errors.Wrap(err, "models: unable to reload all in ExternalPeerSlice")
	}

	*o = slice

	return nil
}

// ExternalPeerExistsG checks if the ExternalPeer row exists.
func ExternalPeerExistsG(ctx context.Context, iD int64) (bool, error) {
	return ExternalPeerExists(ctx, boil.GetContextDB(), iD)
}

// ExternalPeerExists checks if the ExternalPeer row exists.
func ExternalPeerExists(ctx context.Context, exec boil.ContextExecutor, iD int64) (bool, error) {
	var exists bool
	sql := "select exists(select 1 from \"external_peers\" where \"id\"=$1 limit 1)"

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, sql)
		fmt.Fprintln(writer, iD)
	}
	row := exec.QueryRowContext(ctx, sql, iD)

	err := row.Scan(&exists)
	if err != nil {
		return false, errors.Wrap(err, "models: unable to check if external_peers exists")
	}

	return exists, nil
}

// UpsertG attempts an insert, and does an update or ignore on conflict.
func (o *ExternalPeer) UpsertG(ctx context.Context, updateOnConflict bool, conflictColumns []string, updateColumns, insertColumns boil.Columns) error {
	return o.Upsert(ctx, boil.GetContextDB(), updateOnConflict, conflictColumns, updateColumns, insertColumns)
}

// Upsert attempts an insert using an executor, and does an update or ignore on conflict.
// See boil.Columns documentation for how to properly use updateColumns and insertColumns.
func (o *ExternalPeer) Upsert(ctx context.Context, exec boil.ContextExecutor, updateOnConflict bool, conflictColumns []string, updateColumns, insertColumns boil.Columns) error {
	if o == nil {
		return errors.New("models: no external_peers provided for upsert")
	}

	nzDefaults := queries.NonZeroDefaultSet(externalPeerColumnsWithDefault, o)

	// Build cache key in-line uglily - mysql vs psql problems
	buf := strmangle.GetBuffer()
	if updateOnConflict {
		buf.WriteByte('t')
	} else {
		buf.WriteByte('f')
	}
	buf.WriteByte('.')
	for _, c := range conflictColumns {
		buf.WriteString(c)
	}
	buf.WriteByte('.')
	buf.WriteString(strconv.Itoa(updateColumns.Kind))
	for _, c := range updateColumns.Cols {
		buf.WriteString(c)
	}
	buf.WriteByte('.')
	buf.WriteString(strconv.Itoa(insertColumns.Kind))
	for _, c := range insertColumns.Cols {
		buf.WriteString(c)
	}
	buf.WriteByte('.')
	for _, c := range nzDefaults {
		buf.WriteString(c)
	}
	key := buf.String()
	strmangle.PutBuffer(buf)

	externalPeerUpsertCacheMut.RLock()
	cache, cached := externalPeerUpsertCache[key]
	externalPeerUpsertCacheMut.RUnlock()

	var err error

	if !cached {
		insert, ret := insertColumns.InsertColumnSet(
			externalPeerAllColumns,
			externalPeerColumnsWithDefault,
			externalPeerColumnsWithoutDefault,
			nzDefaults,
		)
		update := updateColumns.UpdateColumnSet(
			externalPeerAllColumns,
			externalPeerPrimaryKeyColumns,
		)

		if updateOnConflict && len(update) == 0 {
			return errors.New("models: unable to upsert external_peers, could not build update column list")
		}

		conflict := conflictColumns
		if len(conflict) == 0 {
			conflict = make([]string, len(externalPeerPrimaryKeyColumns))
			copy(conflict, externalPeerPrimaryKeyColumns)
		}
		cache.query = buildUpsertQueryPostgres(dialect, "\"external_peers\"", updateOnConflict, ret, update, conflict, insert)

		cache.valueMapping, err = queries.BindMapping(externalPeerType, externalPeerMapping, insert)
		if err != nil {
			return err
		}
		if len(ret) != 0 {
			cache.retMapping, err = queries.BindMapping(externalPeerType, externalPeerMapping, ret)
			if err != nil {
				return err
			}
		}
	}

	value := reflect.Indirect(reflect.ValueOf(o))
	vals := queries.ValuesFromMapping(value, cache.valueMapping)
	var returns []interface{}
	if len(cache.retMapping) != 0 {
		returns = queries.PtrsFromMapping(value, cache.retMapping)
	}

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, cache.query)
		fmt.Fprintln(writer, vals)
	}
	if len(cache.retMapping) != 0 {
		err = exec.QueryRowContext(ctx, cache.query, vals...).Scan(returns...)
		if err == sql.ErrNoRows {
			err = nil // Postgres doesn't return anything when there's no update
		}
	} else {
		_, err = exec.ExecContext(ctx, cache.query, vals...)
	}
	if err != nil {
		return errors.Wrap(err, "models: unable to upsert external_peers")
	}

	if !cached {
		externalPeerUpsertCacheMut.Lock()
		externalPeerUpsertCache[key] = cache
		externalPeerUpsertCacheMut.Unlock()
	}

	return nil
}
